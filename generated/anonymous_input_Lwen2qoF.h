// callsite
// external/bonsai_stdlib/src/ui/ui.cpp:3728:0

// def (anonymous)
// external/bonsai_stdlib/src/ui/ui.cpp:3728:0



















if (Input->Dot.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('.');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '.';
  }
  Text.Count++;
}

if (Input->Minus.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('-');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '-';
  }
  Text.Count++;
}

if (Input->FSlash.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('/');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '/';
  }
  Text.Count++;
}

if (Input->Space.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper(' ');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = ' ';
  }
  Text.Count++;
}

if (Input->N0.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('0');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '0';
  }
  Text.Count++;
}

if (Input->N1.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('1');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '1';
  }
  Text.Count++;
}

if (Input->N2.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('2');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '2';
  }
  Text.Count++;
}

if (Input->N3.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('3');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '3';
  }
  Text.Count++;
}

if (Input->N4.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('4');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '4';
  }
  Text.Count++;
}

if (Input->N5.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('5');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '5';
  }
  Text.Count++;
}

if (Input->N6.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('6');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '6';
  }
  Text.Count++;
}

if (Input->N7.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('7');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '7';
  }
  Text.Count++;
}

if (Input->N8.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('8');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '8';
  }
  Text.Count++;
}

if (Input->N9.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('9');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = '9';
  }
  Text.Count++;
}

if (Input->A.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('a');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'a';
  }
  Text.Count++;
}

if (Input->B.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('b');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'b';
  }
  Text.Count++;
}

if (Input->C.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('c');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'c';
  }
  Text.Count++;
}

if (Input->D.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('d');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'd';
  }
  Text.Count++;
}

if (Input->E.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('e');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'e';
  }
  Text.Count++;
}

if (Input->F.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('f');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'f';
  }
  Text.Count++;
}

if (Input->G.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('g');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'g';
  }
  Text.Count++;
}

if (Input->H.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('h');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'h';
  }
  Text.Count++;
}

if (Input->I.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('i');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'i';
  }
  Text.Count++;
}

if (Input->J.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('j');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'j';
  }
  Text.Count++;
}

if (Input->K.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('k');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'k';
  }
  Text.Count++;
}

if (Input->L.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('l');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'l';
  }
  Text.Count++;
}

if (Input->M.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('m');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'm';
  }
  Text.Count++;
}

if (Input->N.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('n');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'n';
  }
  Text.Count++;
}

if (Input->O.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('o');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'o';
  }
  Text.Count++;
}

if (Input->P.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('p');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'p';
  }
  Text.Count++;
}

if (Input->Q.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('q');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'q';
  }
  Text.Count++;
}

if (Input->R.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('r');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'r';
  }
  Text.Count++;
}

if (Input->S.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('s');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 's';
  }
  Text.Count++;
}

if (Input->T.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('t');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 't';
  }
  Text.Count++;
}

if (Input->U.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('u');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'u';
  }
  Text.Count++;
}

if (Input->V.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('v');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'v';
  }
  Text.Count++;
}

if (Input->W.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('w');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'w';
  }
  Text.Count++;
}

if (Input->X.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('x');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'x';
  }
  Text.Count++;
}

if (Input->Y.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('y');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'y';
  }
  Text.Count++;
}

if (Input->Z.Clicked)
{
  if (Input->Shift.Pressed)
  {
    Cast(char*, Text.Start)[Text.Count] = ToUpper('z');
  }
  else
  {
    Cast(char*, Text.Start)[Text.Count] = 'z';
  }
  Text.Count++;
}







