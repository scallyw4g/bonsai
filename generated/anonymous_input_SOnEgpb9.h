// external/bonsai_stdlib/src/ui/ui.cpp:3121:0

if (Input->Q.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->W.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->E.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->R.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->T.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->Y.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->U.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->I.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->O.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->P.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->A.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->S.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->D.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->F.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->G.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->H.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->J.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->K.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->L.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->Z.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->X.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->C.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->V.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->B.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->N.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}

if (Input->M.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text[Text.Count] = 'member.name';
  }
  else
  {
    Text[Text.Count] = ToLower('member.name');
  }
  Text.Count++;

  ]
}



