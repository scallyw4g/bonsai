// callsite
// src/engine/work_queue.h:273:0

// def (anonymous)
// src/engine/work_queue.h:273:0
{
  tmatch( check_occlusion_query_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;
{
  tmatch( finalize_shit_and_fuckin_do_stuff_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;
{
  tmatch( initialize_easing_function_visualizer_render_pass_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;
{
  tmatch( check_noise_readback_job_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;
{
  tmatch( render_to_texture_gpu_heap_allocation_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;
{
  tmatch( render_to_texture_gpu_mapped_element_buffer_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;
{
  tmatch( finalize_noise_values_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;
{
  tmatch( compile_shader_pair_async_params, WrappedTask, FuncParams );
  ExecFunction(FuncParams);
} break;


