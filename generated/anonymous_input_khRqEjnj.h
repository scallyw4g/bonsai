// external/bonsai_stdlib/src/ui/ui.cpp:3126:0

if (Input->Q.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYQ;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYQ);
  }
  Text.Count++;

  ]
}

if (Input->W.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYW;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYW);
  }
  Text.Count++;

  ]
}

if (Input->E.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYE;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYE);
  }
  Text.Count++;

  ]
}

if (Input->R.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYR;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYR);
  }
  Text.Count++;

  ]
}

if (Input->T.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYT;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYT);
  }
  Text.Count++;

  ]
}

if (Input->Y.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYY;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYY);
  }
  Text.Count++;

  ]
}

if (Input->U.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYU;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYU);
  }
  Text.Count++;

  ]
}

if (Input->I.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYI;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYI);
  }
  Text.Count++;

  ]
}

if (Input->O.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYO;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYO);
  }
  Text.Count++;

  ]
}

if (Input->P.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYP;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYP);
  }
  Text.Count++;

  ]
}

if (Input->A.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYA;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYA);
  }
  Text.Count++;

  ]
}

if (Input->S.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYS;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYS);
  }
  Text.Count++;

  ]
}

if (Input->D.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYD;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYD);
  }
  Text.Count++;

  ]
}

if (Input->F.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYF;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYF);
  }
  Text.Count++;

  ]
}

if (Input->G.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYG;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYG);
  }
  Text.Count++;

  ]
}

if (Input->H.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYH;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYH);
  }
  Text.Count++;

  ]
}

if (Input->J.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYJ;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYJ);
  }
  Text.Count++;

  ]
}

if (Input->K.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYK;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYK);
  }
  Text.Count++;

  ]
}

if (Input->L.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYL;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYL);
  }
  Text.Count++;

  ]
}

if (Input->Z.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYZ;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYZ);
  }
  Text.Count++;

  ]
}

if (Input->X.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYX;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYX);
  }
  Text.Count++;

  ]
}

if (Input->C.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYC;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYC);
  }
  Text.Count++;

  ]
}

if (Input->V.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYV;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYV);
  }
  Text.Count++;

  ]
}

if (Input->B.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYB;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYB);
  }
  Text.Count++;

  ]
}

if (Input->N.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYN;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYN);
  }
  Text.Count++;

  ]
}

if (Input->M.Clicked)
{
  cs Text = *Ui->TextEdit.Text;
  if (Input->Shift.Pressed)
  {
    Text.Start[Text.Count] = CHARIFYM;
  }
  else
  {
    Text.Start[Text.Count] = ToLower(CHARIFYM);
  }
  Text.Count++;

  ]
}



