in vec2 UV;
in vec3 VertColor;

out vec4 color;

uniform sampler2D TextTextureSampler;

void main()
{
  vec4 BitmapTexel = texture( TextTextureSampler, UV );
  color = vec4(VertColor, BitmapTexel.a);
  /* color = BitmapTexel; */
}
