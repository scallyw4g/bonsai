
        Result |= StringsMatch(ToString(noop), Identifier);

        Result |= StringsMatch(ToString(generate_stream), Identifier);

        Result |= StringsMatch(ToString(generate_cursor), Identifier);

        Result |= StringsMatch(ToString(generate_string_table), Identifier);

        Result |= StringsMatch(ToString(generate_value_table), Identifier);

        Result |= StringsMatch(ToString(for_enum_values), Identifier);

        Result |= StringsMatch(ToString(for_members_in), Identifier);

        Result |= StringsMatch(ToString(d_union), Identifier);

        Result |= StringsMatch(ToString(enum_only), Identifier);

        Result |= StringsMatch(ToString(member_is_or_contains_type), Identifier);

        Result |= StringsMatch(ToString(for_all_datatypes), Identifier);

